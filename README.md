# TSCMボード

TIMESHARINGシリーズ全会場向けの汎用会議室予約管理ボード(Google Apps Script + スプレッドシート)。
GPCMボード(https://github.com/hiroto-saito-droid/GPCMboard )をベースに、会場をマスタ選択式ではなく自由テキスト入力式に変更した汎用版。

実装は `dev` ブランチで進行中。詳細は `dev` ブランチの `CLAUDE.md` / `docs/TSCM_仕様書.md` / `data/README.md` を参照。
